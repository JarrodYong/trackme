{
  "name": "trackme",
  "version": "1.0.0",
  "description": "List of endpoints for the TrackMe API",
  "main": "api.js",
  "scripts": {
    "build": "next build",
    "deploy": "now remove 218271795-sit-209 -y && now -p && now alias",
    "doc": "apidoc -o public/generated-docs/ -e node_modules/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon -r dotenv/config api.js"
  },
  "author": "Jarrod Yong",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "dotenv": "^8.0.0",
    "express": "^4.18.2",
    "mongoose": "^5.6.6",
    "mqtt": "^3.0.0"
  },
  "devDependencies": {
    "apidoc": "^0.17.7",
    "nodemon": "^1.19.1",
    "now": "^16.1.1"
  }
}
